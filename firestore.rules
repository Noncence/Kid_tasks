rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Разрешаем всем читать и писать (для тестирования)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}